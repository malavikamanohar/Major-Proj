# Groq API Keys (comma-separated for fallback)
# You can provide multiple API keys for automatic fallback
# Example: GROQ_API_KEYS=key1,key2,key3
GROQ_API_KEYS=your-groq-api-key-here

# Or use single key (legacy support)
# GROQ_API_KEY=your-groq-api-key-here

# LLM Configuration
LLM_MAX_RETRIES=3
LLM_TIMEOUT=60
